== README

1. Configure Gmail credentials:

- Rename the configuration file localated in "/config/config_files/email.example.yml" by "email.yml"
- Replace the data with an email credential

2. Installing gems

- Open a terminal and go to project
- Run the command "bundle install"
- Wait for installing all gems

3. Database

- Located in the project run the command "rake db:migrate"